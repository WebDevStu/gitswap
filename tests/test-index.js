"use strict";

var chai = require('chai'),
    index = require('../index'),
    should = chai.should();

describe('index', function () {

    it('should pass', function (done) {
        done();
    });
});